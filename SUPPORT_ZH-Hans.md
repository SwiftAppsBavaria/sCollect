# sCollect 帮助

## 这个 App 做什么

sCollect 管理媒体收藏 —— 音乐、影片、家庭录像、有声书、播客、电子书 —— 以及并非媒体文件
的收藏品，比如硬币或邮票。类别叫什么名字，你可以在**设置 → 类别标签**里自己决定。

你在编辑器里输入的内容，App 会**写回文件本身**，而不只是写进它自己的目录。

## 第一步

1. 首次启动时为媒体库选择一个文件夹。目录数据日后就放在那里；如果你愿意，媒体文件也放在
   那里。
2. 通过**文件 → 导入文件**（⌘O）或**文件 → 导入文件夹**（⇧⌘O）把文件或文件夹拿进来，或者
   直接拖到窗口上。
3. 导入时你每次都可以决定，文件是**拷贝到媒体库**，还是只是**链接**。

## 已管理还是已链接？

| | |
|---|---|
| **已管理** | 文件位于媒体库中。sCollect 会把它归类、按你的方案重命名，并写入标签。 |
| **已链接** | 文件留在原处。sCollect 记住它的位置，并且**不会动这个文件**。 |

列表中的“已链接”列会显示是哪一种。

## 常见问题

**某个条目带着橙色警告三角形。**
它的文件在上一次**文件 → 媒体库 → 标记缺失的项目**运行时找不到。这样的条目无法编辑 —— 没有地方可以
写入。右键菜单里 App 会提供**查找文件…**；找到的文件会被取回媒体库。

**右键菜单里写着“驱动器未连接”，而且是灰的。**
那么丢的不是文件，而是硬盘。sCollect 明确区分这两种情况：没有连接的东西，它也无法检查 ——
因此也不会把它标记为缺失。请接上硬盘，然后重新运行这次检查。

**某个媒体类型是灰的，无法更改。**
它的文件夹目前无法访问。sCollect 会锁定这样的类型，而不是悄悄把文件放到别处。硬盘一回来，
锁定就结束了。

**编辑器显示“1 个字段与文件不一致”。**
文件在某个字段里写着与媒体库不同的内容 —— 多半是因为期间有别的程序编辑过它。字段上方的横幅
会显示涉及哪些字段，而你可以逐个字段决定，是否采用文件里的值。

**我找不到我需要的字段。**
收藏品有三个可以自由命名的字段。它们该叫什么，你在**设置 → 字段标签**里按类别设定。

**我能把 iTunes 或音乐的收藏接过来吗？**
可以。sCollect 会读取 iTunes-XML，连同评分和播放列表。曲目是通过文件路径来对应的；已有的评分
不会被覆盖。

**我怎么把收藏再导出来？**
通过 **sCollect-XML** —— 它是无损的，同时也可以当作备份。此外还可以导出为 iTunes-XML，以及
Apple Music 的播放列表。

**与副本的同步是做什么的？**
一个媒体库可以把其他媒体库注册为副本。主库上的更改会被带到那里去，文件和标签都包括在内。
如果某个副本当前离线，更改会先留着，稍后补上。

⚠️ **这不是备份。** 被删除的文件在副本里也会被删除 —— 这正是同步的用意。万一出了岔子，你还
需要一份真正的备份。

**写标签会让我的文件损失质量吗？**
不会。音频和图像数据原样保留，不会重新编码。只要做得到，sCollect 就只改标签的那几个字节，
而不是重写整个文件。

**需要备份吗？**
需要。sCollect 写入的是你的文件本身，而不是它们的副本。在一次性大幅改动大量条目之前，请先备份；用 Time Machine 就够了。

**我能撤销一次更改吗？**
被删除的条目用 ⌘Z 就能找回来。元数据的更改不行 —— 所以在大批量处理之前，请先做一份备份。

## 出了问题？

sCollect 会在你媒体库的文件夹里写一份日志 —— 它记下 App 在什么时候做了什么。报告错误时，
欢迎把它一并发来。

## 联系方式

SwiftAppsBavaria · SwiftAppsBavaria@gmx.net
